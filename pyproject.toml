[project]
name = "prometheus-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.90.0",
    "python-dotenv>=1.1.1",
    "vllm>=0.10.0",
]
